# meta developer: @bezzubik_modules и @space_modules
# version: 1.0.0

from .. import loader, utils
import io
import base64
import aiohttp

@loader.tds
class GeminiGuessMod(loader.Module):
    """Угадывает слово на картинке через Gemini API без лишних сообщений"""
    strings = {"name": "AutoCroko"}

    def __init__(self):
        self.config = loader.ModuleConfig(
            loader.ConfigValue(
                "api_key",
                None,
                "🔑 API ключ Gemini (Google AI Studio)",
                validator=loader.validators.String(),
            ),
            loader.ConfigValue(
                "model",
                "gemini-2.5-pro",
                "🤖 Модель Gemini Vision",
                validator=loader.validators.String(),
            ),
            loader.ConfigValue(
                "prompt",
                "Определи, что изображено на картинке, и ответь одним простым словом. Рассмотри абсолютно все детали, которые присутствуют на изображении: форму объектов, линии, углы, текстуры, цвета, тени, надписи, стрелки, подписи, объекты вокруг основного предмета, а также общий контекст сцены. Если изображён предмет, назови его простым и общим словом, понятным большинству людей. Примеры: монитор, контейнер, консоль, дверь, дом, дерево, мяч и так далее. Не используй сложные термины или узкоспециализированные названия. Если изображён человек, определяй его эмоцию в обобщённой форме: радость, грусть, злость, страх, удивление, спокойствие. Также пробуй описывать действия или детали лица, только общее эмоциональное состояние. Если на изображении есть стрелки, линии или текстовые элементы, используй их как подсказки для понимания назначения объекта, направления или функции, но не повторяй текст буквами. Пусть это помогает уточнить объект. Обязательно оценивай форму и материал объектов: если объект прямоугольный и металлический — вероятнее всего это контейнер; если объект пористый и мягкий — скорее всего это ткань, подушка или что-то похожее; если объект круглый и маленький — возможно это мяч; если объект прозрачный или с экраном — скорее всего это монитор или окно; если на объекте поезд и из трубы идёт дым — это паровоз, независимо от других деталей. Оценивай контекст сцены: окружающие предметы, фон, расположение объектов. Иногда контекст важнее визуальных деталей для правильного определения. Если объект трудно точно определить, выбери самое вероятное и простое слово, которое описывает категорию или тип объекта. Не придумывай новые термины, не добавляй описаний. Ответ всегда строго одно слово. Не используй прилагательные, действия, объяснения, повтор текста, описания деталей или любые дополнительные комментарии. Только одно слово, максимально точное и понятное. Если изображён цветок, трава или любое растительное явление, обязательно определи его конкретный тип, используя широкий словарный запас и точные названия, а не общее слово «Цветок». Примеры конкретных названий: Роза, Одуванчик, Подсолнух, Тюльпан, Ромашка, Василёк, Лилия, Пион, Орхидея, Кактус. Используй форму лепестков, центральную часть и стебель для точной идентификации. При анализе учитывай стиль, которому ты был обучен на 8 рисунках: Стиль: подавляющее большинство рисунков выполнено как минималистичный, чёрно-белый, контурный скетч (набросок). Линии часто неровные и толстые, создавая эффект рисования от руки (hand-drawn) на чистом белом фоне. Цвет: цвет используется крайне редко, только для выделения ключевых деталей (например, красный центр на мишени для дартса), но основной стиль — монохромный контур. Специфические объекты, которые были изучены: бумага с волнистыми линиями и числами — Чек; простая фигурка человека в структуре с перекладинами — Качели; простое насекомое с овальным телом и шестью ногами — Жук; бумага с текстом, указывающим на секретность — Тайна; бумага с текстом (> SYSTEM32) — Консоль; простая фигурка водоплавающей птицы — Уточка; две фигурки рядом с вертикальной фигурой с крестом — Похороны; круглая мишень с секторами и красным центром — Дартс; ветвь с листьями и мелкими ягодами (как на приведённом изображении) — Рябина. Ответ всегда должен быть строго ОДНО СЛОВО.",
                "🧠 Вопрос, который отправляется модели",
                validator=loader.validators.String(),
            ),
        )

    async def угадайcmd(self, message):
        """Использование: .угадай <реплай на фото>"""
        reply = await message.get_reply_message()
        if not reply or not reply.media:
            return await message.delete()  # удаляем команду, если нет картинки

        api_key = self.config["api_key"]
        model = self.config["model"]
        prompt = self.config["prompt"]

        if not api_key:
            await message.edit("⚠️ Укажи API ключ в конфиге: `.cfg AutoCroko`")
            return

        try:
            img_bytes = await reply.download_media(bytes)
            img_b64 = base64.b64encode(img_bytes).decode("utf-8")

            url = f"https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent"

            payload = {
                "contents": [{
                    "parts": [
                        {"text": prompt},
                        {"inline_data": {"mime_type": "image/png", "data": img_b64}},
                    ]
                }]
            }

            headers = {
                "Content-Type": "application/json",
                "x-goog-api-key": api_key,
            }

            async with aiohttp.ClientSession() as session:
                async with session.post(url, headers=headers, json=payload) as resp:
                    data = await resp.json()

            if "candidates" not in data:
                await message.delete()
                return

            text = data["candidates"][0]["content"]["parts"][0]["text"].strip()

            # Удаляем команду и сразу отправляем ответ
            await message.delete()
            await message.client.send_message(message.to_id, f" {text}")

        except Exception:
            await message.delete()
# поздаравляю весь наш код :) 